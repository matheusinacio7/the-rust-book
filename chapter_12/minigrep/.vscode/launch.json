{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "lldb",
      "request": "launch",
      "name": "Debug program",
      "program": "${workspaceFolder}/target/debug/minigrep",
      "args": ["boDy", "poem.txt"],
      "cwd": "${workspaceFolder}",
      "preLaunchTask": "build"
    }
  ]
}